## Below is the order in which you run the tests

- First run

```bash
"npm run node-render" to start the node server on "http//:localhost:3000".
```

- Then run

```bash
  "npm run dev" to start the nextjs development server.
```
